language: cpp
compiler:
  - clang++
  - gcc
addons:
  apt:
    sources:
      - boost-latest
      - ubuntu-toolchain-r-test
      - llvm-toolchain-trusty-6.0
    packages:
      - libboost-all-dev
      - libssl-dev
      - cmake
      - libstdc++-7-dev

script: cmake CMakeLists.txt && make
